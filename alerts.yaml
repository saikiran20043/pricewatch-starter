# Alert rules evaluated by the watcher after every scan.
rules:
  # Fire when a product's price drops by at least `pct` percent versus its previous observation.
  - type: drop_pct
    pct: 10
  # Fire when the current price is at least 15 percent below the trailing median.
  - type: below_median
    pct: 15
    window_days: 30
